
import { customPropertyType } from 'client/helpers/custom-property-type';
import { module, test } from 'qunit';

module('Unit | Helper | custom property type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = customPropertyType([42]);
  assert.ok(result);
});

