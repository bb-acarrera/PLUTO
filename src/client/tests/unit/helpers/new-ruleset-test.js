
import { newRuleset } from 'client/helpers/new-ruleset';
import { module, test } from 'qunit';

module('Unit | Helper | new ruleset');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = newRuleset([42]);
  assert.ok(result);
});

