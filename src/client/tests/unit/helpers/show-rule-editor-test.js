
import { showRuleEditor } from 'client/helpers/show-rule-editor';
import { module, test } from 'qunit';

module('Unit | Helper | show rule editor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showRuleEditor([42]);
  assert.ok(result);
});

