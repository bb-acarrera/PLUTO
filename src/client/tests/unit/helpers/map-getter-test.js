
import { mapGetter } from 'client/helpers/map-getter';
import { module, test } from 'qunit';

module('Unit | Helper | map getter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapGetter([42]);
  assert.ok(result);
});

