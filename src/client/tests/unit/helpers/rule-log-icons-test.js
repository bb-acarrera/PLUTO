
import { ruleLogIcons } from 'client/helpers/rule-log-icons';
import { module, test } from 'qunit';

module('Unit | Helper | rule log icons');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ruleLogIcons([42]);
  assert.ok(result);
});

