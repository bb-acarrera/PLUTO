
import { ruleLogRow } from 'client/helpers/rule-log-row';
import { module, test } from 'qunit';

module('Unit | Helper | rule log row');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ruleLogRow([42]);
  assert.ok(result);
});

