
import { createObject } from 'client/helpers/create-object';
import { module, test } from 'qunit';

module('Unit | Helper | create object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createObject([42]);
  assert.ok(result);
});

